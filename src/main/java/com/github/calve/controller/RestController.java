package com.github.calve.controller;

//@Controller
public class RestController { // TODO review of methods, some methods hasn't direct sql queries

  /*  @Autowired
    private VoteRepository jdmr;

    public Set<MenuItem> getAllDishes(LocalDate date, Integer id) {
        Menu menu = jdmr.getMenuByDateAndRestaurant(date, id);
        Set<MenuItem> items = menu.getItems();
        return items != null ? menu.getItems() : null;
    }

    public Menu getMenu(LocalDate date, Integer id) {
        return jdmr.getMenuByDateAndRestaurant(date, id);
    }

    public void updateDishes(List<MenuItem> list) {

    }

    public List<Menu> getMenus(LocalDate date) {
        List<Menu> menus = jdmr.getMenuListByDate(date);
        return menus;
    }

    public List<VoteLog> getVotes() {
        return jdmr.getVoteLogs();
    }

    public void vote(LocalDate date, Integer rest_id) {
        jdmr.vote(date, rest_id);
    }

    public void unlockVoiting(Integer userId) {
        jdmr.unlockVoting(userId);
    }

    public void getRestourantHistory(Integer rest_id) {

    }

    public List<HistoryItem> getHistory() {
        return jdmr.getAllHistory();
    }

    public void updateHistory() {
        jdmr.updateHistory();
    }*/

}
